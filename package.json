{
  "name": "aula-de-letras",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "cd client && npm install && cd ../server && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
